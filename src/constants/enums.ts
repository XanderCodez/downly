export enum TabId {
  DOWNLOADS = 'DOWNLOADS',
  SETTINGS = 'SETTINGS',
  LOGS = 'LOGS',
}

export enum Theme {
  DARK = 'DARK',
  LIGHT = 'LIGHT',
  SYSTEM = 'SYSTEM',
}

export enum DownloadStatus {
  IDLE = 'IDLE',
  PREPARING = 'PREPARING',
  DOWNLOADING = 'DOWNLOADING',
  PROCESSING = 'PROCESSING',
  COMPLETED = 'COMPLETED',
  ERROR = 'ERROR',
  CANCELLED = 'CANCELLED',
}

export enum DownloadType {
  VIDEO = 'VIDEO',
  AUDIO = 'AUDIO',
}

export enum YtDlpFormatType {
  VIDEO = 'VIDEO',
  AUDIO = 'AUDIO',
}

export enum Direction {
  LTR = 'ltr',
  RTL = 'rtl',
}

export enum Language {
  ENGLISH = 'en',
  ARABIC = 'ar',
}
